package com.gerenciamento.produtos;

//Arquivo de teste de CRUDs de produtos no banco de dados
public class TestProductDAO {
    public static void main(String[] args) {
        // Cria uma instância de ProductDAO
        // ProductDAO productDAO = new ProductDAO();
        // CategoryDAO categoryDAO = new CategoryDAO();

        // Category category = categoryDAO.getCategoryById(1); // Busca a categoria
        // correspondente ao id passado
        // Cria um novo produto para inserir no banco de dados
        // Product product = new Product("Lasanha", "Deliosa Lasanha da casa, com nosso
        // molho especial de 4 queijos",
        // 24.90, 10, category);

        // Insere o produto no banco de dados
        // productDAO.insertProduct(product);

        // ProductManager.detailsProduct(product);

        // Buscar o produto pelo ID
        // long productIdToUpdate = 3; // id a ser localizado
        // long productIdToDelete = 1; // id a ser deletado
        // Product productToUpdate = productDAO.findProductById(productIdToUpdate);
        // productDAO.removeProduct(productIdToDelete);

        // if (productToUpdate != null) {
        // productToUpdate.setName("Fanta Lata 350ml");
        // productToUpdate.setDescription(productToUpdate.getDescription());
        // productToUpdate.setPrice(5.99);
        // productToUpdate.setQuantity(10);
        // productToUpdate.setCategory(category);
        // productDAO.updateProduct(productToUpdate);

        // ProductManager.detailsProduct(productToUpdate);
        // } else {
        // System.out.println("Produto não encontrado");
        // }

        // Chama o método de inserção do ProductDAO
        // productDAO.insertProduct(product);

        // if (productToUpdate != null) {
        // ProductManager.detailsProduct(productToUpdate);
        // } else {
        // System.out.println("Produto não encontrado");
        // }

    }

}
